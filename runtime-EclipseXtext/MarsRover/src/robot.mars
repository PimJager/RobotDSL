Behaviors: 
	DriveForward
	DetectLine

Stops when: False

Implementation for DriveForward:
	Takes control when: True
	Does:
		Forward;
		
Implementation for DetectLine:
	Takes control when: LeftLight < 3/10 || RightLight < 3/10
	Does:
		Stop; 

