Behaviors: 
	DriveForward
	DetectLine

Stops when: False

Implementation for DriveForward:
	Takes control when: True
	Does:
		Forward;
		
Implementation for DetectLine:
	Takes control when: LeftLight < 30 || RightLight < 30
	Does:
		Stop; 

